const canvas = document.getElementById('flowLines');
const ctx = canvas.getContext('2d');

const {width, height} =  b._el.getBoundingClientRect();
canvas.style.width = width + 'px';
canvas.style.height = height + 'px';